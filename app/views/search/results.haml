- @title = "Results for '#{@querystring}'"

.container
  .row
    .span9
      - unless @results.empty?
        %b= @results.total
        =I18n.t("search.results")
        %span.muted (#{@results.time} ms)

        = partial 'search/result', :collection => @results
        .pagination-centered
          = paginate @results, :window => 3

      - else
        = I18n.t("search.empty")

      %br
    .span3
      - unless @results.facets.nil? or @results.empty? or 1 == @results.size
        .well.sidebar-nav
          %ul.nav.nav-list
            = partial 'search/facet', :collection => @results.facets